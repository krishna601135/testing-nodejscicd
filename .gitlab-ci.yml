workflow:
  rules: 
    - if: $CI_COMMIT_BRANCH != "main" && $CI_PIPELINE_SOURCE != "merge_request_event"
      when: never
    - when: always


run_unit_tests:
  tags:
    - docker
    - local
  before_script:
    - cd app
    - npm install
  script:
     - npm test
